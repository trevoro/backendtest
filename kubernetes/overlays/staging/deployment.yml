apiVersion: apps/v1
kind: Deployment
metadata:
  name: test99
spec:
  template:
    spec:
      containers:
        - name: test99
          resources:
            requests:
              memory: 128Mi
              cpu: 0.1
            limits:
              memory: 512Mi
              cpu: 1.0
